;;; gv-user.el --- user config
;;; Commentary:
;;; sets up user specific configuration

;;; Code:

(defconst gv-user-name "your name" "user name")
(defconst gv-user-email "your email" "user email")

(defconst gv-user-rust-src-dir "~/tools/src/rust/src" "path to rust src dir")

(provide 'gv-user)
;;; gv-user.el ends here
