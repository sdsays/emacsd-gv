;;; gv-org.el --- setup org
;;; Commentary:
;;; sets up Org editing environment

;;; Code:

;; fontify code in code blocks
(setq org-src-fontify-natively t)

;; (require 'ox-moderncv)
;; (require 'ox-altacv)

(provide 'gv-org)
;;; gv-org.el ends here
